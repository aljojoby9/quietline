# sqlite (default for local `go run ./cmd/server`)
QUIETLINE_DSN=sqlite:quietline.db
QUIETLINE_LISTEN=:8080

# postgres (docker compose)
# QUIETLINE_DSN=postgres://quietline:quietline@127.0.0.1:5432/quietline?sslmode=disable

# client
# QL_SERVER=http://127.0.0.1:8080
# QL_HOME=/tmp/ql-alice
